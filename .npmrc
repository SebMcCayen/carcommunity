# pnpm workspace configuration
#
# link-workspace-packages=true makes pnpm resolve local workspace packages
# by version range (compatible with the existing ^x.y.z version specs used
# across this monorepo before workspace: protocol was adopted).
link-workspace-packages=true
