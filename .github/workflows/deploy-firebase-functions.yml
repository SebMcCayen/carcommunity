name: Deploy Firebase Functions

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy-functions:
    name: Deploy to production
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: deploy-functions-production
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud via Workload Identity Federation
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/functions/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: apps/functions
        run: pnpm install --frozen-lockfile

      - name: Build
        working-directory: apps/functions
        run: pnpm run build

      - name: Deploy Cloud Functions
        working-directory: apps/functions
        run: pnpm exec firebase deploy --only functions --config ../../firebase/firebase.json --project kungsbacka-car-community --non-interactive
