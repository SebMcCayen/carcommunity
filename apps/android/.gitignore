
# Release signing secrets — never commit (keystore lives outside the repo)
keystore.properties
*.jks
*.keystore

# Mapbox runtime token — never commit (repo is public); local builds only
mapbox.properties
