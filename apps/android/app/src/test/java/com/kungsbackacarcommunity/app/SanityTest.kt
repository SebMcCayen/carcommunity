package com.kungsbackacarcommunity.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test proving the JUnit target compiles and runs (Phase 5).
 * Real domain tests arrive with the first feature slices.
 */
class SanityTest {
    @Test
    fun `test target is wired`() {
        assertEquals(4, 2 + 2)
    }
}
